services:
  - type: web
    name: rent-tax-backend
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: rent_tax_db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
